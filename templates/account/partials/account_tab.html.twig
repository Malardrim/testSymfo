{% block account_data_content %}
    <div class="row">
        <div class="col-12">
            {{ form_start(form) }}
            {{ form_row(form) }}
            <button class="btn btn-primary">Submit</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
