{% extends 'base.html.twig' %}

{% block title %}Edit Rule{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-1 my-2">
                <a class="btn btn-primary" role="button" href="{{ path('rule_index') }}"><i class="fas fa-arrow-left"></i> Back</a>
                {{ include('rule/_delete_form.html.twig') }}
            </div>
            <div class="col-lg-10 py-2">
                <h1>Edit Rule</h1>
                {{ include('rule/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>
{% endblock %}
