{% extends 'base.html.twig' %}

{% block title %}New Rule{% endblock %}

{% block body %}
    <a class="btn btn-primary position-sticky float-left m-5" role="button" href="{{ path('rule_index') }}"><i class="fas fa-arrow-left"></i> Back</a>
    <div class="container">
        <div class="row">
            <div class="col-lg-12 py-2">
                <h1>Create new Rule</h1>
                {{ include('rule/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
