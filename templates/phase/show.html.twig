{% extends 'base.html.twig' %}

{% block title %}Phase{% endblock %}

{% block body %}
    <h1>Phase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phase.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phase.name }}</td>
            </tr>
            <tr>
                <th>Priority</th>
                <td>{{ phase.priority }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('phase_index') }}">back to list</a>

    <a href="{{ path('phase_edit', {'id': phase.id}) }}">edit</a>

    {{ include('phase/_delete_form.html.twig') }}
{% endblock %}
