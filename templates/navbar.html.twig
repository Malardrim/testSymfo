<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">
        <ul class="navbar-nav mr-auto my-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('rule_index') }}">Rules</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('faction_index') }}">Factions</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('phase_index') }}">Phases</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('item_index') }}">Items</a>
            </li>
        </ul>
    </div>
    <div class="mx-auto order-0">
        <a class="navbar-brand mx-auto" href="{{ path('homepage') }}">Homepage</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        <ul class="navbar-nav ml-auto my-auto">
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item">
                    <span class="nav-link active">Hello {{ app.user.username }}</span>
                </li>
                <li class="nav-item ml-1">
                    <a class="text-decoration-none" href="{{ path('app_logout') }}" ><button class="btn btn-primary btn-raised">Logout</button></a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>